pipeline {
  agent any
  environment {
    PATH = "$PATH:/opt/apache-maven-3.9.4/bin"
  }

  stages {
    stage('CLEAN WORKSPACE') {
      steps {
        cleanWs()
      }
    }
    stage('Checkout') {
      steps {
        sh 'echo passed'
        git branch: 'main', url: 'https://github.com/imtiazalijamil/argocd.git'
      }
    }
    stage('Build and Test') {
      steps {

        sh 'ls -ltr'
        // build the project and create a JAR file
        sh 'cd spring-boot-app && mvn clean package'
      }
    }
    stage('Static Code Analysis') {
      environment {
        SONAR_URL = "http://10.0.137.46:9000"
      }
      steps {
        withCredentials([string(credentialsId: 'sonarqube', variable: 'SONAR_AUTH_TOKEN')]) {
          sh 'cd spring-boot-app && mvn sonar:sonar -Dsonar.login=$SONAR_AUTH_TOKEN -Dsonar.host.url=${SONAR_URL}'
        }
      }
    }

    stage('Update Deployment File') {
      environment {
        GIT_REPO_NAME = "argocd"
        GIT_USER_NAME = "imtiazalijamil"
      }
      steps {
        withCredentials([string(credentialsId: 'github', variable: 'GITHUB_TOKEN')]) {
          sh '''
          git config user.email "m.iaj@outlook.com"
          git config user.name "imtiazalijamil"
          BUILD_NUMBER=${BUILD_NUMBER}
          pwd
          cat spring-boot-app-manifests/deployment.yml
          sed -i "s+ahmedonw/ultimate-cicd.*+ahmedonw/ultimate-cicd:${BUILD_NUMBER}+g" spring-boot-app-manifests/deployment.yml
          git add spring-boot-app-manifests/deployment.yml
          git commit -m "Update deployment image to version ${BUILD_NUMBER}"
          git push https://${GITHUB_TOKEN}@github.com/${GIT_USER_NAME}/${GIT_REPO_NAME} HEAD:main
          '''
        }
      }
    }
  }
}